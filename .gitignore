# Obsidian workspace-local state (churns across devices)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Obsidian caches / plugin runtime
.obsidian/cache
.trash/

# OS junk
.DS_Store
Thumbs.db

# Nested git repo — Edbot v2 coordination meta-repo (its own git repo; not part of the vault)
edbot-workspace/
